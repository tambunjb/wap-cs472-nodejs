const add = (x, y) => x + y;

const substract = (x, y) => x - y;

const multiply = (x, y) => x * y;

const divide = (x, y) => x / y;

const pi = 3.14;

module.exports = {
    add: add,
    substract: substract,
    multiply: multiply,
    divide: divide,
    pi: pi
};